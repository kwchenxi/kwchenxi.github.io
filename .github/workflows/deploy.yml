name: Force GitHub Pages Deployment

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Force GitHub Pages Deployment
        run: echo "Triggering GitHub Pages deployment..."